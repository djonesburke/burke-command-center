[build]
builder = "nixpacks"
buildCommand = "rm -rf .next && npm run build"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npx next start -p ${PORT:-3000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
